INCLUDE_DIRECTORIES(
    ${SMEDIA_INC_DIRS}
    )

IF(${SMEDIA_PLATFORM} STREQUAL win32)
    ADD_DEFINITIONS(
        -DUART_EXPORTS
        )
ENDIF(${SMEDIA_PLATFORM} STREQUAL win32)

ADD_LIBRARY(uart
    mmp_uart.c
    )

IF(${SMEDIA_PLATFORM} STREQUAL win32)
    TARGET_LINK_LIBRARIES(uart
        mmp
        )
ENDIF(${SMEDIA_PLATFORM} STREQUAL win32)
